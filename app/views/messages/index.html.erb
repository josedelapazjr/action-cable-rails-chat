<h1>Messages</h1>
<ul>
  <% @messages.each do |message| %>
    <li><%= message.content %></li>
  <% end %>
</ul>

<%= form_for(@message, remote: true) do |f| %>
  <%= f.text_area :content %>
  <%= f.submit "Send" %>
<% end %>
