<h1>Messages</h1>
<div id='messageList'>
  <ul>
    <% @messages.each do |message| %>
      <li><%= message.content %></li>
    <% end %>
  </ul>
</div>

<%= form_for(@message, remote: true) do |f| %>
  <%= f.text_area :content %>
  <%= f.submit "Send" %>
<% end %>
